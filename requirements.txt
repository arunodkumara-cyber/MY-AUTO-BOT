# Run this to create a clean requirements.txt file
libs = ["streamlit", "pandas", "numpy", "plotly", "requests"]

with open("requirements.txt", "w", encoding="utf-8") as f:
    for lib in libs:
        f.write(lib + "\n")
print("Done! requirements.txt is ready.")
